apiVersion: v1
kind: Service
metadata:
  name: {{ include "chrisalvis-me.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "chrisalvis-me.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chrisalvis-me.selectorLabels" . | nindent 4 }}
